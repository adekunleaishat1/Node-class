<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css">
    <title>Document</title>
</head>
<body>

    <div class="container">
        <div class="row">
           <div class="col-8 card mx-auto py-4 px-5">
              <div class="d-flex justify-content-center align-items-center ">
                 <h6 class="display-6 text-muted">Todo Application</h6>  
              </div>
              <form action="/todo/createtodo" method="post" class="w-100">
                <input placeholder="title" class="form-control w-100 my-2" name="title" type="text">
                <input placeholder="text" class="form-control w-100 my-2" name="text" type="text">

                <button class="btn btn-outline-dark my-2">submit</button>
             </form>
             <div class=" my-4 d-flex justify-content-center align-items-center flex-column ">
                <% for( let i=0; i < todoarray.length; i++ ) { %>
                    <div class="card w-100 my-2">
                        <p>Title: <%= todoarray[i].title %></p>
                        <p>Text: <%= todoarray[i].text %></p>
                        
                        <div class="d-flex">
                            <form action="/todo/deletetodo" method="post">
                                <input type="hidden" name="index" value="<%= todoarray[i]._id%>">
                                <button class="btn btn-dark">Delete</button>
                            </form>
                            <form action="/todo/edit/<%= todoarray[i]._id %>" method="get">
                                <button class="btn btn-dark mx-3">Edit</button>
                            </form>
                        </div>
                    </div>
                    <% } %>
             </div>
           </div>
        </div>
    </div>
</body>
</html>